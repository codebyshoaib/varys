# Secret token files — never commit these
.claude/hooks/.notion
.claude/hooks/.slack
.claude/hooks/.linkedin
.claude/hooks/.axiom

# User session data — personal to each install
vault/logs/*
!vault/logs/.gitkeep
.beads/failures.jsonl

# Python
*.pyc
__pycache__/

# OS
.DS_Store

# Repo symlinks — these point to sibling repos, not tracked here
repos/
